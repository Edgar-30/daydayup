# Repository working rules

- 用中文汇报。先读 README.md、docs/PROGRESS.md、当天任务和 git status。
- 每次只实现一个主要目标，最多两项配套任务；保留用户每天约 1 小时审阅时间。
- 不覆盖用户未提交改动，不重写远端历史，不制造空提交、倒签日期或为了贡献图拆碎提交。
- 用户已授权本仓库项目范围内的代码编写、调试、正常提交与推送；未授权操作其他仓库或个人资料。
- 改行为前写清输入、输出、错误和统计口径，运行与风险相称的相关测试。
- 当前验证命令：python -m unittest discover -s tests -v。
- 更新真实开发记录；明确区分代码完成、用户理解、文件修改、本地提交、远端推送和远端 CI。
- 没运行的检查必须标为未验证，不能凭工作流文件宣称 CI 通过。
- 每日提供独立完整的执行提示词、用户验证步骤和两道与实现直接相关的问题。
- 用户交作业后，分别判断成果和理解，最多三项改进；没有证据时不替用户填写掌握情况。
- 下次先处理未完成范围，不机械叠加功能；第 2 周以后按周滚动规划。
